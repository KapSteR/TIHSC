/*
 * Empty C++ Application
 */

#include <math.h>

void move(float poseAngle, int distance, float heading) {

	if (fmod(heading-poseAngle,M_TWOPI) >= fmod(poseAngle-heading,M_TWOPI)) {  // Left turn

	} else { // Right turn

	}
}

int main() {
	return 0;
}
