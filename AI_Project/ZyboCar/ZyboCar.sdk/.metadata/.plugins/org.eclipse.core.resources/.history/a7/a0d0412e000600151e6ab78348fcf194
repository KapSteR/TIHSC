#ifndef PATHMAKER_H_
#define PATHMAKER_H_
#include "Position.h"
#include "MoveBlock.h"

#include "Utility.h"

class PathMaker{
public:
	PathMaker();
    
	vector< vector<int> > map;
    vector< vector<int> > path;
    vector< vector<int> > aStarHeuristic;
    
	Position goal;

	void performAStar(Position &robotPosition);
	void getNextMove(MoveBlock &MB);
	bool checkGoal(Position &robotPosition);
	void setGoal(Position pos);
    void loadMap();
    
private:
    void makeHeuristic();

    vector<float> possibleTurns = { M_PI_2, M_PI, M_PI+M_PI_2, 0 };
};

#endif /* PATHMAKER_H_ */
