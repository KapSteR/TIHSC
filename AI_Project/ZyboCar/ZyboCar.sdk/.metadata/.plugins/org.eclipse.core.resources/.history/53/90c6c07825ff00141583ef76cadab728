/*
 * Empty C++ Application
 */

#include "xparameters.h"
#include "xmotorctrl.h"

int main() {

	char inString[4];
	char inputByte;
	int pwmLeft, pwmRight;

	while (1) {

		xil_printf("CMD:>");

		inputByte = inbyte();

		if (inputByte == 'R'){

		}

		else if (inputByte == 'L' ) {

		}




		for(int i; i < 4 ; ++i) {
			inputByte = inbyte();

			if (inputByte == '\n' || inputByte == '\r') {
				break;
			}

			else if (){

			}

			else {
				inString[i] = inputByte;
			}
		}








	}

	return 0;
}
